RegistrationResult:
  description: OK
  required: [data]
  properties:
    data:
      $ref: '#/RegistrationResultData'

RegistrationResultData:
  type: object
  required: [result]
  properties:
    result:
      type: boolean
      example: true